node_modules
.next
out
.env.local
.env*.local
npm-debug.log*

.vercel
.env*

*.tsbuildinfo

# public/shared-engine/*.js É versionado (ver scripts/sync-shared.js):
# o deploy do Pro na Vercel empacota só o diretório pro/, então esses
# arquivos precisam existir aqui pra chegar em produção — sync-shared.js
# só consegue regerá-los localmente (onde js/ da raiz do repo existe).
# Nunca editar à mão; rodar "node scripts/sync-shared.js" pra atualizar.
